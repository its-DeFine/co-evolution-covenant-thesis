# Contracts (reference code)

This folder contains a minimal reference implementation of the on-chain anchor described in `spec/authorship-registry.contract.md`.

**Status**: draft reference code; not audited; do not use in production without review.

## Build (Foundry)

Install Foundry (if needed): https://book.getfoundry.sh/getting-started/installation

Compile:

```bash
forge build
```

